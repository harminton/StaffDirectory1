@model IEnumerable<StaffDirectory.Models.DepartmentStaff>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Departmentid)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Staff)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Departmentid)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Staff.StaffID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DepartmentStaffID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DepartmentStaffID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DepartmentStaffID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
